[project]
name = "swap-connector"
authors = ["swap-team"]
description = "Cross-chain liquidity routing and swap connector"
telemetry = false
requirements = []
[contracts.swap-connector]
path = "contracts/swap-connector.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
